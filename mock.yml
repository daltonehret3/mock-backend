rest:
  test:
    GET:
      route: "/:id"
      params:
        id: String
    POST:
      route: "/"
      body:
        name: String
        email: String
    
  another:
    GET:
      route: "/"
models:
  test:
    id:
      unique: true
      type: UUID
    name: 
      type: String
    email:
      type: Email
