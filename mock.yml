rest:
  test:
    GET:
      route: "/:id"
      params:
        id: String
    POST:
      route: "/"
      body:
        name: String
        email: String
  test/:id/projects:
    GET:
      route: "/"
  another:
    GET:
      route: "/"

